"""Throwaway stand-ins for the other pairs' modules.

Everything in here returns hardcoded but VALID objects, so the orchestrator
and the Streamlit app can be built and debugged before anyone else has
delivered anything.

These also double as the reference implementation for Pairs B and C: if your
real function returns what the stub returns, it will drop straight in.

Delete this package once every real module is live.
"""

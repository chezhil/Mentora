# Python 3.11 or newer (the code uses PEP 604 `X | None` at runtime).

# Core
pydantic>=2.6
streamlit>=1.31
matplotlib>=3.8
numpy>=1.26
python-dotenv>=1.0             # app.py and smoke_test.py read .env

# Pair A — ingest/ (Utkarsh)
pymupdf>=1.24
python-docx>=1.1
python-pptx>=0.6
chromadb>=0.4
sentence-transformers>=2.7     # loads BAAI/bge-m3
# FlagEmbedding>=1.2           # alternative BGE-M3 loader; ingest/ uses
                               # sentence-transformers, so this is not needed

# Pair B — planner/ teacher/ (Jyothi, Naman)
google-genai>=1.0              # NOT google-generativeai
openai>=1.40                   # Groq and Ollama speak the OpenAI API

# Pair C — prompt_101/media_pipeline (Santosh, Hamza)
imageio-ffmpeg>=1.4            # NOT a system ffmpeg
# These four were imported by code that runs on every lesson but were never
# declared. They are pulled in transitively by streamlit/matplotlib/torch, so
# the repo worked by accident; a fresh install that resolved differently lost
# the diagram and concept-map renderers (networkx), every PNG save (Pillow),
# and Wav2Lip's audio front end (scipy).
Pillow>=10.0                   # renderers save_figure, avatar photo checks
networkx>=3.0                  # diagram and concept_map renderers
scipy>=1.11                    # vendor/wav2lip audio
requests>=2.31                 # downloads the Replicate render

# Local avatar (local_avatar/) — free alternative to Replicate
torch>=2.2                     # Wav2Lip inference in local_avatar/
opencv-python-headless>=4.9    # FaceDetectorYN, video writing
librosa>=0.10                  # mel spectrogram for Wav2Lip
# Weights are NOT in the repo (436MB). See README "Avatar weights".
piper-tts>=1.7                 # local voice: en, hi, te. Offline, no key.
edge-tts>=7.0                  # free neural voice for ta, kn, bn, mr and as
                               # fallback everywhere. No key, needs network.
# Voice models are NOT in the repo (63MB each). See README "Voice models".
# REPLICATE_API_TOKEN env var  # real avatar; without it it is a still image
# replicate>=0.25              # ONLY for the paid Replicate avatar path. The
                               # local Wav2Lip backend needs no account, so
                               # this stays optional and its import is guarded.

fastapi>=0.110.0
uvicorn>=0.28.0

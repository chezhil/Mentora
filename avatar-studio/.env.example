# The default engine (wav2lip-local) is free and needs NO key.
#
# Optional paid upgrade: the cloud SadTalker engine on Replicate
# (~$0.05 per render). To use it, uncomment and fill in:
#   AVATAR_ENGINE=replicate
#   REPLICATE_API_TOKEN=rk_...

# Port the site listens on (default 8000).
# PORT=8000
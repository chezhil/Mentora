[mypy]
files = jobs.py, tests/test_jobs.py
follow_imports = skip
# Tests exercise policy, not annotation hygiene — every other check still
# applies to both files.
disable_error_code = no-untyped-def
